name: Deploy to GitHub Pages

on:
  push:
    branches: 
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist
  
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

5. Scroll down and click the green **"Commit changes"** button

6. Click **"Commit changes"** again in the popup

---

## Step 3: Change GitHub Pages Settings

1. Click **"Settings"** at the top of your repository

2. In the left sidebar, scroll down and click **"Pages"**

3. Under **"Build and deployment"**, find **"Source"**

4. Click the dropdown and select **"GitHub Actions"** (NOT "Deploy from a branch")

5. That's it - no need to click Save, it saves automatically

---

## Step 4: Run the Build

1. Click **"Actions"** tab at the top of your repository

2. You should see a workflow running (yellow dot)

3. Wait 2-3 minutes for it to finish (will turn green ✅)

4. Once green, go to: **https://veritasvoid.github.io/TradeZen/**

5. Your app will be live! 🎉

---

## Visual Guide

**What you're doing:**
```
Your Repo
├── (all your files)
└── .github/           ← You're creating this
    └── workflows/     ← And this
        └── deploy.yml ← And adding this filename: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'
  
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

5. Scroll down and click the green **"Commit changes"** button

6. Click **"Commit changes"** again in the popup

---

## Step 3: Change GitHub Pages Settings

1. Click **"Settings"** at the top of your repository

2. In the left sidebar, scroll down and click **"Pages"**

3. Under **"Build and deployment"**, find **"Source"**

4. Click the dropdown and select **"GitHub Actions"** (NOT "Deploy from a branch")

5. That's it - no need to click Save, it saves automatically

---

## Step 4: Run the Build

1. Click **"Actions"** tab at the top of your repository

2. You should see a workflow running (yellow dot)

3. Wait 2-3 minutes for it to finish (will turn green ✅)

4. Once green, go to: **https://veritasvoid.github.io/TradeZen/**

5. Your app will be live! 🎉

---

## Visual Guide

**What you're doing:**
```
Your Repo
├── (all your files)
└── .github/           ← You're creating this
    └── workflows/     ← And this
        └── deploy.yml ← And adding this file
